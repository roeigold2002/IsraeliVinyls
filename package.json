{
  "name": "vinyl-store",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.cjs",
    "dev:e2e": "node scripts/dev.cjs --host 127.0.0.1 --port 5173",
    "export:snapshot": "(python -m scripts.export_snapshot || python3 -m scripts.export_snapshot) && node scripts/build_search_bundle.cjs",
    "bundle:search": "node scripts/build_search_bundle.cjs",
    "ingest:all": "python -m scripts.ingest_all_stores || python3 -m scripts.ingest_all_stores",
    "verify:coverage": "python -m scripts.verify_store_coverage || python3 -m scripts.verify_store_coverage",
    "verify:record-integrity": "node scripts/verify_record_integrity.cjs",
    "index:manual-html": "node scripts/index_manual_html.cjs",
    "verify:manual-index": "node scripts/verify_manual_index.cjs",
    "manual-index:refresh": "npm run index:manual-html && npm run verify:manual-index",
    "remediate:prices": "node scripts/remediate_prices.cjs",
    "remediate:assets": "node scripts/remediate_assets.cjs",
    "trueup:run": "node scripts/trueup_worker.cjs",
    "trueup:loop": "node scripts/trueup_worker.cjs --loop",
    "trueup:rebuild-queue": "node scripts/trueup_worker.cjs --rebuild-queue",
    "rebuild:stats": "node scripts/rebuild_stats.cjs",
    "remediate:run": "npm run remediate:prices && npm run remediate:assets && npm run export:snapshot && npm run verify:record-integrity && npm run verify:snapshot-integrity && npm run verify:api-contract",
    "prices:refresh": "node scripts/remediate_prices.cjs --concurrency 50 --timeout-ms 3000 && npm run export:snapshot && npm run rebuild:stats && npm run verify:record-integrity && npm run verify:api-contract && npm run deploy:netlify:prod",
    "verify:snapshot-integrity": "node scripts/verify_snapshot_integrity.cjs",
    "verify:api-contract": "node scripts/verify_api_contract.cjs",
    "synthetic:check": "node scripts/synthetic_checks.cjs",
    "rollout:staged": "node scripts/staged_rollout.cjs --policy rollout/rollout-policy.json",
    "test:unit": "node --test tests/unit/*.test.cjs",
    "test:integration": "node --test tests/integration/*.test.cjs",
    "test:e2e": "playwright test",
    "test:e2e:ci": "playwright test --reporter=line",
    "qa:gate": "npm run export:snapshot && npm run verify:record-integrity && npm run verify:snapshot-integrity && npm run test:unit && npm run test:integration && npm run verify:api-contract && npm run test:e2e:ci && npm run build",
    "snapshot:refresh": "npm run export:snapshot && npm run verify:record-integrity && npm run verify:coverage && npm run verify:api-contract",
    "build:android:web:fast": "vite build --base ./ --outDir dist/mobile",
    "build:android:web": "npm run export:snapshot && npm run verify:record-integrity && npm run verify:api-contract && tsc -b && npm run build:android:web:fast",
    "prepare:android:web-to-app": "node scripts/prepare_android_web_bundle.cjs",
    "build:android:handoff:fast": "npm run build:android:web:fast && npm run prepare:android:web-to-app",
    "build:android:handoff": "npm run build:android:web && npm run prepare:android:web-to-app",
    "build": "npm run export:snapshot && npm run verify:record-integrity && npm run verify:api-contract && tsc -b && vite build",
    "preview": "vite preview",
    "preflight:netlify": "npm run build && npm run verify:snapshot-integrity && npm run verify:api-contract",
    "deploy:netlify:preview": "npx netlify-cli deploy --build",
    "deploy:netlify:prod": "npx netlify-cli deploy --build --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.0",
    "lucide-react": "^1.8.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "react-router-dom": "^7.14.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@tailwindcss/vite": "^4.2.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "tailwindcss": "^4.2.2",
    "typescript": "^6.0.2",
    "vite": "^8.0.8"
  }
}
